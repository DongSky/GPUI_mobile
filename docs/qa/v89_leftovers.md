# v90 leftovers — GPUI mobile Material 3 Expressive

Continuing from **v89** (`feat: v89 Expressive DatePickerHeadline Picker empty Selected date`). Critique vs [m3.material.io](https://m3.material.io/components/date-pickers/overview) Expressive.

**v89 landed on `main` only (no PR).** Start from that tip; do not re-open DatePickerHeadline Picker empty (`Selected date`, `PICKER_EMPTY` / `month_grid_unselected` / `date_headline(Picker, None)`, catalog `data-picker-empty` / `datepicker-picker-empty`, hosts `date_picker_empty_card` / `android_date_picker_empty`), DateRangePickerHeadline empty (`Start date – End date`, `RANGE_EMPTY_HEADLINE` / `header_range_selection(empty())`, catalog `data-range-input-empty` / `data-range-headline-empty` / `datepicker-range-input-empty`, hosts `date_range_input_empty_card` / `android_date_range_input_empty`), DatePickerHeadline Input empty (`Entered date`, `INPUT_EMPTY_HEADLINE` / `date_headline`, catalog `data-date-input-empty` / `data-date-headline-empty` / `datepicker-input-empty`, hosts `date_input_empty_card` / `android_date_input_empty`), DateRangePicker Input SelectableDates not-allowed (`Date not allowed: Sat, Sep 12`, catalog `data-range-error="allowed"` / `data-date-range-allowed-start`, hosts `date_range_input_error_card` / `android_date_range_input_errors`, `range_input_error` after year-range), calendar-grid weekend disable (`DayKind::Disabled`, `SELECTABLE_DATES_GRID`, `day_accepts_tap`, catalog `data-selectable-dates` / `data-kind="Disabled"`, hosts skip weekend taps), SelectableDates / Date not allowed (`Date not allowed: Sat, Sep 12`, `SELECTABLE_DATES`, `is_selectable_date` weekends, `DateInputError::NotAllowed`, `date_input_error` / `range_input_error` after year-range, catalog `data-date-error="allowed"` / `INPUT_ERROR_NOT_ALLOWED_SAMPLE`), DatePicker Input DateInputValidator errors (`Date format not recognized` / year-range, `DATE_INPUT_ERRORS`, `date_input_error`, catalog `data-date-input-errors` / `datepicker-input-errors`), DateInputValidator year-range (`Date out of expected year range 1900 - 2100`, `INPUT_ERROR_YEAR_RANGE` / `INPUT_ERROR_YEAR_SAMPLE`, `DateInputError::YearRange`, `range_input_error` after format and before order, catalog `data-range-error="year"`), DateRangePicker Input format/order errors (`Date format not recognized` / `End date can't be before start date`, `RANGE_INPUT_ERRORS`, `range_input_error`, catalog `data-range-input-errors` / `datepicker-range-input-errors`), docked trailing DateRange (Compose `Icons.Default.DateRange` on the outlined field, `DOCKED_TRAILING`, `apply_docked_toggle`, catalog `data-docked-trailing`, hosts `toggle_docked_open`), range-hero VerticalMonthsList (2-month window, titleSmall month subhead, `CalendarMonthSubheadPadding` 24/20/8, catalog `data-range-vertical-months` / `data-range-months` / `data-range-subhead`, desktop/Android stacked months), docked live select (tap InMonth day writes the outlined field + dismisses, `DOCKED_LIVE_SELECT`, `apply_docked_tap`, catalog `data-docked-select-live` / `data-docked-dismiss-select`, hosts `select_docked_day`), docked YearPicker (month ▾ opens 3×72×36 `YearPicker` on the docked popup, `apply_docked_year`, catalog `data-docked-year-toggle` / `data-docked-years` / `data-docked-pane`, `docked_pane` independent of the modal / range hero), single-date modal month nav (prev/next, `apply_date_month` YearRange clamp, catalog `data-date-month-nav` / `data-date-month-delta`, independent of the range hero), single-date modal Confirm/Cancel (draft until OK, Cancel restores committed, header divider, catalog `data-date-ok` / `data-date-cancel` / `data-date-actions-live` / `data-date-grid`, docked still commits on select), range-hero `drawRangeBackground` half-cell start/end connectors (`RangeFill` StartHalf / EndHalf / Full, catalog `data-range-connector` / `data-range-fill`, desktop/Android half-bar + selected circle), range-hero Confirm/Cancel (draft until OK, Cancel restores committed, header divider, catalog `data-range-ok` / `data-range-cancel` / `data-range-divider`), range-hero showModeToggle (Picker calendar ↔ Input Start/End fields, `range_title_for` / `range_field_value`, catalog `data-range-display-live` / `data-range-display-toggle`, independent of the single-date modal; range-input sibling stays), range-hero YearPicker (month ▾ opens 3×72×36 `YearPicker` on the range calendar, `apply_range_year`, catalog `data-range-year-toggle` / `data-range-years`), range-hero month nav (prev/next, `apply_range_month` YearRange clamp, catalog `data-range-month-nav` / `data-range-month-delta`, cross-month InRange), live range start→end selection (Compose `DateRangePicker` tap: start, then end ≥ start, third tap restarts; catalog `data-date-range-live`, desktop/Android range hero days), modal date range input (Compose `DateRangePicker` `DisplayMode.Input`, Enter dates, Start/End outlined MM/DD/YYYY, catalog `datepicker-range-input` sibling), YearPicker sibling (3×72×36 pills, YearRange 1900–2100, month ▾ opens year pane, catalog `datepicker-year` sibling), live Picker↔Input `showModeToggle` on the modal (catalog `data-date-display-live`, desktop/Android `date_display` + edit/calendar swap), modal date input (Compose `DatePickerDisplayMode.Input`, outlined MM/DD/YYYY, catalog `datepicker-input` sibling), search filter chips, search no-results empty, two-line leading 40/20, trailing clear-X, two-line queried search rows, segmented 2dp filled search rows, TimePickerLayoutType Horizontal, docked search 32% scrim + min 240 / max ⅔, search Results / Quick results status, 24-hour ClockFace dial, search suggestion groups, contained compact→docked, 24-hour TimeInput, TimeInput 96×72 extra-large + ScrollDisplayModeToggle, contained search, Expressive TimeScroll, `roundedShape` CornerMedium 12 + `tonalColors()` + Inside label, list swipe LazyColumn fling, hideOnCollapse always-extended Create, header-less modal/narrow Extended FAB Create, in-flow Extended FAB Create, ContentPadding 0/44/0/44, modalExpandedShape CornerLarge 16 / ModalContainerColor SurfaceContainer, header Menu/MenuOpen + Extended FAB Create + tooltip Above, Arrangement.Bottom, hideOnCollapse slide, Arrangement.Center, in-page submenu cascade autofocus, live narrow modal 80↔220, in-flow standard WideNavigationRail 96↔220, modal collapsed width 96, live Top→Start iconPosition lerp, GPUI chip `with_animation` press, chip press interpolation / InputChip avatar / typeahead autofocus, or GPUI hover-open as immediate, or overflow/split as a single 16dp shell, or ElevatedFilterChip / unscrimmed overlay as missing inventory — they ship. Navigation drawer and **banner** stay **out** of inventory.

Highest-impact remaining gaps:

1. **Live JVM IME (`jstring`)** — host `jni_text_from_arg` uses UTF-8 `GetStringUTFChars` on Android; a live `ImeListener` on a device JVM still needs NDK + `jni-sys`.
2. **GPUI caret bounds** — `crates/gpui` has no `Window` IME-rect API. A real GPUI method needs a crate bump.
3. **Search-view morph** — AndroidX search morphs with a container transform. GPUI still has no `cx.transform`; contained expand uses height/margin/corner tokens only (no PathBuilder scale). Compact/docked is a layout swap, not a shared-element scale.
4. **`gpui::LineCap`** — `progress.rs` maps `StrokeCap::Round` to lyon until gpui re-exports `LineCap`.
5. **Nav rail `PopUp`** — OS `PopUp` on Linux/Android NativeActivity is still unproven; rail uses an inline GPUI overlay.
6. **Device APK** — no SDK/AVD/adb in this environment; `component_demo` still needs `cargo apk run` on a device/emulator.
7. **Live GPUI + Chrome recapture** — this VM cannot create a wgpu surface; Chrome `--screenshot` hangs (dump-dom works). Official-vs-catalog strips remain v19 left halves. Recapture HTML heroes (including DatePickerHeadline Picker empty + DateRangePickerHeadline empty + DatePickerHeadline Input empty + range Input SelectableDates not-allowed + Picker SelectableDates weekend disable + DatePicker Input not-allowed + format/year-range + range Input year-range + format/order + docked trailing DateRange + range-hero VerticalMonthsList + docked live select + docked YearPicker + modal month nav + Confirm/Cancel + range-hero connectors + Confirm/Cancel + showModeToggle + YearPicker + month nav + live range start→end + date range input + YearPicker + live date Picker↔Input + modal date input + filter chips + no-results empty + 40/20 leading + trailing clear-X + two-line results + segmented search rows + horizontal TimePicker + docked search scrim + queried Quick results / Results + 24-hour ClockFace) when a display/screenshot pipeline works.
8. **Predictive-back** stays unimplemented. Modal rail predictive-back scale (`PredictiveBackMaxScaleXDistance` 24 / `Y` 48) needs `cx.transform` or an equivalent host scale. Nested Android `LazyColumn` overscroll glow is not in GPUI. Higher-res stills (current bundle is 128–240px) if official optical match needs print-size photos.

Do **not** reopen as missing: JPEG `PhotoKind::jpeg_bytes` / decode / mosaic / **licensed camera stills**, snackbar peek + dest SVGs, Your lists chrome, split/FAB-menu/toolbar token modules, medium/large **flexible** collapse + search app bar, side sheet **standard/modal/detached**, navigation **drawer** (deprecated), **banner** (not on current site), plain/rich **tooltip** + **caret** + **hover/long-press**, flexible **64dp** nav, **segmented lists**, **standard + connected** button groups, standard **OverflowIndicator**, list **swipe rails** + **drag-handle reorder** + **LazyColumn fling**, icon-button **narrow/default/wide**, icon-button **toggle selected** colors + round↔square morph, **Expressive menus**, **Expressive Filter/Input chip morph**, **ElevatedFilterChip** + **tonal** leading-icon `onSurfaceVariant`, **InputChip 24dp avatar** + compact 4dp, **live chip press interpolation**, **WideNavigationRailItem Top/Start**, **live Top→Start iconPosition lerp**, **in-flow standard WideNavigationRail**, **in-flow Extended FAB Create**, **modal collapsed width 96**, **live narrow modal 80↔220**, **header-less modal/narrow Extended FAB Create**, **hideOnCollapse always-extended Create**, **submenu flyout**, **overlay submenu**, **unscrimmed anchored overlay**, **overflow/split grouped 2dp + nested More flyout**, **GPUI hover-open timer**, **typeahead autofocus**, **hideOnCollapse** dismissible modal, **Arrangement.Center**, **header Menu/MenuOpen** + **Extended FAB Create** + **tooltip Above** + **Arrangement.Bottom**, **modalExpandedShape CornerLarge 16** + **ModalContainerColor SurfaceContainer**, **ContentPadding 0/44/0/44**, **Expressive text field roundedShape CornerMedium 12** + **tonalColors** + **Inside**, **Expressive TimeScroll**, **contained search**, **contained compact→docked**, **search suggestion groups**, **search Results / Quick results status**, **docked search 32% scrim** + **min 240 / max ⅔**, **search segmented result rows**, **two-line queried search rows**, **search trailing clear-X**, **search two-line leading 40/20**, **search no-results empty state**, **search filter chips**, **TimeInput** 96×72 extra-large + **ScrollDisplayModeToggle**, **24-hour TimeInput**, **24-hour ClockFace dial**, **TimePickerLayoutType Horizontal**, **modal date input** (DisplayMode.Input, outlined MM/DD/YYYY, calendar/edit toggle), **live Picker↔Input showModeToggle**, **YearPicker** (3×72×36, YearRange 1900–2100, month ▾), **modal date range input** (DateRangePicker Input, Start/End outlined MM/DD/YYYY), **live range start→end** (DateRangePicker tap selection on the range hero), **range-hero month nav** (prev/next, YearRange clamp, cross-month InRange), **range-hero YearPicker** (month ▾, 3×72×36, independent of the single-date modal), **range-hero showModeToggle** (Picker ↔ Input Start/End; range-input sibling stays), **range-hero Confirm/Cancel** (draft until OK, header divider), **range-hero drawRangeBackground connectors** (StartHalf / EndHalf / Full, catalog `data-range-fill`), **single-date modal Confirm/Cancel** (draft until OK, header divider; docked still commits on select), **single-date modal month nav** (prev/next, YearRange clamp, independent of the range hero), **docked YearPicker** (month ▾, 3×72×36, independent of the modal / range hero), **docked live select** (tap writes field + dismisses, `apply_docked_tap`), **range-hero VerticalMonthsList** (2-month window, titleSmall subhead, CalendarMonthSubheadPadding 24/20/8), **docked trailing DateRange** (outlined-field icon button, `apply_docked_toggle`), **DateRangePicker Input supporting-text errors** (`Date format not recognized` / `End date can't be before start date`, `range_input_error`), **DateInputValidator year-range** (`Date out of expected year range 1900 - 2100`, `DateInputError::YearRange`), **DatePicker Input DateInputValidator errors** (`date_input_error`, format / year-range, catalog `datepicker-input-errors`), **SelectableDates / Date not allowed** (`is_selectable_date` weekends, `DateInputError::NotAllowed`), **calendar-grid weekend disable** (`DayKind::Disabled`, `day_accepts_tap`, catalog `data-kind="Disabled"`), **DateRangePicker Input SelectableDates not-allowed** (catalog `data-range-error="allowed"`, hosts range Input error cards), **DatePickerHeadline Input empty** (`Entered date`, catalog `datepicker-input-empty`), **DateRangePickerHeadline empty** (`Start date – End date`, catalog `datepicker-range-input-empty`), **DatePickerHeadline Picker empty** (`Selected date`, catalog `datepicker-picker-empty`).

## Suggested v90 order

1. Live IME / caret / search transform / `gpui::LineCap` if an NDK or gpui API bump becomes available (otherwise skip).
2. Highest remaining Visual QA gap that does not need a crate bump (OS `PopUp` and predictive-back scale stay skipped if still unproven / no `cx.transform`). Implementable catalog/host inventory is exhausted without a crate bump — prefer recapture or a newly published Expressive spec delta.
3. APK install + Chrome/wgpu recapture of v21–v89 heroes.

Host tests: `cargo test -p gpui_material` (35), `-p gpui_android` (17), `-p material_desktop_demo` (1). Android GPUI paint is `cfg(target_os = "android")`.
