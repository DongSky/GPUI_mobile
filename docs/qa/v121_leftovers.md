# v122 leftovers — GPUI mobile Material 3 Expressive

Continuing from **v121** (`feat: v121 Expressive DatePicker MonthsNavigation a11y`). Critique vs [m3.material.io](https://m3.material.io/components/time-pickers/specs) Expressive.

**v121 landed on `main` only (no PR).** Start from that tip; do not re-open DatePicker MonthsNavigation a11y (`MONTH_NAV_A11Y` / `PREV_MONTH` `Change to previous month` / `NEXT_MONTH` `Change to next month` / `SWITCH_TO_YEAR` `Switch to selecting a year` / `SWITCH_TO_DAY` `Swipe to select a year, or tap to switch back to selecting a day` / `year_toggle_label` / `DatePickerPane::year_toggle_label`, catalog `data-month-nav-a11y` + title / aria-label on prev/next and year menu + live JS pane flip, hosts hover tooltip on live date / range / docked month arrows and year toggles — unofficial “Previous month” / “Next month” retired), TimePickerDialogDefaults `ScrollDisplayModeToggle` SwipeVertical (`SWIPE_VERTICAL` / `SWIPE_VERTICAL_ICON` ⇅ / `display_mode_toggle_icon`, Scroll↔Input Keyboard / SwipeVertical, catalog `data-swipe-vertical` + live ⌨/⇅, hosts `toggle_icon` — unofficial Input Schedule ◷ retired on the scroll toggle; `DisplayModeToggle` Input→Picker still Schedule), DatePicker `DisplayModeToggleButton` a11y (`DISPLAY_MODE_TOGGLE` / `TOGGLE_INPUT` `Switch to text input mode` / `TOGGLE_CALENDAR` `Switch to calendar input mode`, catalog `data-date-display-mode-toggle` + title / aria-label + live JS, hosts hover tooltip on live date / range toggles — do not restore shorter “Switch to input mode” / “Switch to calendar mode”), TimePickerDialogDefaults `DisplayModeToggle` / `ScrollDisplayModeToggle` a11y (`TOGGLE_KEYBOARD` / `TOGGLE_SCROLL` / `TOGGLE_TOUCH` / `DISPLAY_MODE_TOGGLE` / `display_mode_toggle_label`, catalog `data-display-mode-toggle` / `data-toggle-keyboard` / `data-toggle-scroll` / `data-toggle-touch` + title / aria-label, hosts hover tooltip with `toggle_label()` — official “Switch to text input mode” / “Switch to scroll mode” / “Switch to clock mode”; keep title-row 12/24 + ScrollDisplayModeToggle), PeriodSelector 1dp Outline shell (`PERIOD_OUTLINE` / `PERIOD_OUTLINE_W_DP` 1 / `PERIOD_CONTAINER_H_DP` 80 / `PERIOD_H_DP` 40 / `PERIOD_GAP_DP` 0 / `period_outline_w_css`, appearance `period_outline` Outline, catalog `data-period-outline` + 1px Outline CornerSmall 8 shell on dial / TimeScroll / TimeInput, hosts bordered CornerSmall 8 no item gap — 52×80 vertical / 216×38 horizontal / TimeInput 52×72; unofficial 8dp gap + 36dp buttons retired; do not apply `periodSelectorShape` CornerFull; do not add TimeSelector 2dp selected outline), TimeSelector selected / idle colors (`TIME_SELECTOR_COLORS` / `time_selector_selected_container` PrimaryContainer / `time_selector_selected` OnPrimaryContainer / `time_selector_container` SurfaceContainerHighest / `time_selector_content` OnSurface, catalog `data-time-selector-colors` + hour selected / minute idle + `setDial` paint, hosts TimeSelector fields — clock numbers stay Primary / OnPrimary; keep CornerSmall 8; do not add 2dp selected outline), TimeInput vibrant field outline (`TIME_FIELD_FOCUS_OUTLINE_W_DP` 2 / `TIME_FIELD_UNFOCUSED_OUTLINE_W_DP` 1 / `TIME_FIELD_OUTLINE` / `time_field_outline_w_css`, `TimeInputAppearance.field_outline` Transparent / `field_focused_outline` Primary, `resolve_input` SurfaceContainerLowest containers + OnSurface idle text + Primary focused text, catalog `data-time-field-outline` + hour 2px Primary / minute 1px Transparent + focus/blur paint, hosts `desktop_time_input_field` / `android_time_input_field` `border_2` / `border_1` — keep 96×72 CornerLarge 16), TimePickerDefaults `shapes()` / `ScrollFieldDefaults.shape` CornerLarge (`TIME_PICKER_SHAPES` / `TIME_FIELD_SHAPE_CORNER_DP` 16 / `SCROLL_FIELD_CORNER_DP` / `INPUT_FIELD_CORNER_DP` / `time_field_shape_corner_css`, catalog `data-time-picker-shapes` / `data-time-field-shape="large"`, hosts `field_corners` 16 — unofficial ExtraLarge 28 retired on TimeScroll / TimeInput fields), SearchBar `fullScreenContainedSearchBarColor` (`FULL_SCREEN_CONTAINED_COLOR` / `full_screen_contained_container` surfaceContainerLow / `contained_container_for` FullScreen expanded, catalog `data-search-fullscreen-color`, compact Android hero; docked + collapsed stay `contained_container` surfaceContainerHigh), DatePicker `WeekDays` (`WEEKDAYS_LABEL` / `WEEKDAY_ROW_MIN_H_DP` 48 / `weekday_row_min_height_css`, `resolve` weekday OnSurface + BodyLarge, catalog `data-date-weekdays` / `data-date-weekday` 48dp row, hosts weekday cells 48dp + BodyLarge — day cells stay 40), VibrantTimePickerDialog (`VIBRANT_DIALOG` / `VIBRANT_DIALOG_CORNER_DP` 28 / `vibrant_dialog_container` surfaceContainer / `vibrant_dialog_on_container` onSurface / `vibrant_dialog_toggle` onSurfaceVariant, catalog `data-time-vibrant-dialog`, hosts expressive TimeScroll/TimeInput shell), TimePicker `ClockFaceSizeModifier` (`TIME_PICKER_MAX_HEIGHT_DP` 384 / `TIME_PICKER_MID_HEIGHT_DP` 330 / `CLOCK_DIAL_MID_CONTAINER_SIZE_DP` 238 / `CLOCK_DIAL_MIN_CONTAINER_SIZE_DP` 200 / `CLOCK_DIAL_SIZES` / `ClockDialSize` / `clock_dial_size_for_max_height` / `clock_dial_container_size_css` / `DEMO_HOST_CLOCK_MAX_HEIGHT_DP` 320 / `DEMO_HOST_CLOCK_SIZE` Min / `demo_host_clock_dp`, catalog `data-clock-dial-sizes` / `data-clock-dial-size` max 256 / mid 238 / min 200 / `data-hero="timepicker-clock-mid"` / `timepicker-clock-min`, hosts `demo_host_clock_dp` 200 + `NUMBER_DP` 48 — unofficial 192 / 0.75 scale retired), DatePickerDialog `DialogButtonsCrossAxisSpacing` 8 (`DIALOG_BUTTONS_CROSS_GAP_DP` 8 / `dialog_buttons_cross_gap_css`, catalog `data-date-dialog-buttons` flex-wrap + row-gap 8, hosts date Cancel/OK `flex_wrap`), TimePickerCustomLayout (portrait title top 24 / actions bottom 24, landscape title 24 / content top 16 / content-actions 4 / actions bottom 8, `PORT_TITLE_TOP_DP` / `PORT_ACTIONS_BOTTOM_DP` / `LAND_TITLE_TOP_DP` / `LAND_CONTENT_TOP_DP` / `LAND_CONTENT_ACTIONS_DP` / `LAND_ACTIONS_BOTTOM_DP` / `DIALOG_ACTIONS_GAP_DP` 8 / `DIALOG_ACTIONS` / `DIALOG_OK` / `DIALOG_CANCEL` / `actions_bottom_dp`, catalog `data-time-dialog-actions` / `data-time-dialog-layout` / `.time-body` / `.time-dialog-actions`, hosts `desktop_time_dialog_actions` / `android_time_dialog_actions`), TimePickerDialogDefaults `Title` (Picker/Scroll `Select time`, Input `Enter time`, `TITLE` / `SCROLL_TITLE` / `INPUT_TITLE` / `TITLE_PAD_BOTTOM_DP` 20 / `DIALOG_TITLE` / `title_for` / `title_pad_bottom_css` + appearance labelMedium, catalog `data-time-dialog-title` / `.time-dialog-title` / 20dp `.time-display-head` pad, hosts expressive `title_for(mode)` + 20dp bottom / dial Picker title 20dp), TimeInput `SupportLabelTop` 7 / Hour / Minute supporting text (`SUPPORT_LABEL` / `SUPPORT_LABEL_TOP_DP` / `INPUT_HOUR_LABEL` / `INPUT_MINUTE_LABEL` / `support_label_top_css` / `ScrollKind::support_label`, catalog `data-time-support-label` / `.time-input-support`, hosts `desktop_time_input_column` / `android_time_input_column` + bodySmall / on-surface-variant), YearPicker trailing `HorizontalDivider` (`YEAR_PICKER_DIVIDER` / `YEAR_PICKER_DIVIDER_H_DP` / `year_picker_divider_height_css` / `year_picker_divider_visible`, catalog `data-date-year-divider` / `.dp-year-divider` after `.dp-years` on live / range / docked / year sibling — hidden while calendar or input, hosts `year_picker_divider` / `android_year_picker_divider` when year pane is open), DateEntryContainer header `HorizontalDivider` (under title/headline/toggle, `DATE_ENTRY_DIVIDER` / `DATE_ENTRY_DIVIDER_H_DP` 1 / `date_entry_divider_visible` / `date_entry_divider_height_css`, catalog `data-date-entry-divider` / `.dp-entry-divider`, hosts `date_entry_divider` / `android_date_entry_divider` + outline-variant; docked omitted), DatePickerDialog `ContainerWidth` 360 / `ContainerHeight` 568 (`CONTAINER_W_DP` / `CONTAINER_H_DP` / `CONTAINER_SIZE`, catalog `data-date-container` / `.cal` max 360×568, hosts modal cards 360×568 — docked stays field-width), `DatePickerModalTokens.HeaderContainerHeight` 120 / `RangeSelectionHeaderContainerHeight` 128 / `HeaderHeightOffset` 60 (`HEADER_CONTAINER_H_DP` / `RANGE_HEADER_CONTAINER_H_DP` / `RANGE_HEADER_HEIGHT_OFFSET_DP` / `RANGE_HEADER_MIN_H_DP` 68 / `HEADER_CONTAINER_HEIGHTS` / `header_min_height_css` / `range_header_min_height_css`, catalog `data-date-header-min` / `data-date-range-header-min`, hosts apply 120 / 68 min-heights), TimePicker `DisplaySeparatorWidth` 24 / `PeriodToggleMargin` 12 (`DISPLAY_SEPARATOR_W_DP` / `DISPLAY_SEPARATOR_H_DP` 80 / `INPUT_DISPLAY_SEPARATOR_H_DP` 72 / `DISPLAY_SEPARATOR` / `PERIOD_TOGGLE_MARGIN_DP` / `PERIOD_TOGGLE_MARGIN`, catalog `data-display-separator` / `data-period-toggle-margin`, dial + TimeInput colons; TimeScroll colon unchanged), TimePicker `ClockDisplayBottomMargin` 36 / `ClockFaceBottomMargin` 24 (`CLOCK_DISPLAY_BOTTOM_MARGIN_DP` / `CLOCK_FACE_BOTTOM_MARGIN_DP` / `CLOCK_FACE_MARGINS`, catalog `data-clock-face-margins`, vertical dial only — horizontal 24dp `HORIZONTAL_GAP_DP` stays), MonthsNavigation `MonthYearHeight` 56 / `RecommendedSizeForAccessibility` 48 (`MONTH_YEAR_H_DP` / `MONTH_NAV_ICON_DP` / `MONTH_YEAR_NAV`, catalog `data-date-month-year` / `.month-nav-arrows`, year menu at start + 48dp prev/next at end, year-pane hides arrows), DateRangePicker range-header chrome (leading close/back in the official 64dp start slot, `RANGE_HEADER_CHROME` / `RANGE_HEADER_CLOSE` / `RANGE_HEADER_CLOSE_*` / `apply_range_header_close`, catalog `data-range-header-chrome` / `data-range-header-close`, hosts `range_header_close` / `android_range_header_close` + live dismiss), DatePickerDialog `DialogButtonsPadding` (end 6 / bottom 8, `DIALOG_BUTTONS_PAD_*` / `DIALOG_BUTTONS_MAIN_GAP_DP` 8 / `DIALOG_BUTTONS_CROSS_GAP_DP` 8 / `DIALOG_BUTTONS_PADDINGS` / `dialog_buttons_padding_css`, catalog `data-date-dialog-buttons`), DateRangePickerTitlePadding / DateRangePickerHeadlinePadding (start 64 / end 12 / bottom 12, `RANGE_TITLE_PAD_*` / `RANGE_HEADLINE_PAD_*` / `RANGE_HEADER_PADDINGS`), DatePickerModeTogglePadding (end 12 / bottom 12), Date Input `InputTextFieldPadding` / `InputTextNonErroneousBottomPadding` (start 24 / end 24 / top 10 / bottom 16), DateRangePickerHeadline end-only (`Start date – Sep 21`), DatePickerTitlePadding / DatePickerHeadlinePadding (start 24 / end 12 / top 16 / bottom 12), DateRangePickerHeadline start-only (`Sep 15 – End date`), DateRangePickerTitle Picker empty (`Select dates`), DatePickerHeadline Picker empty (`Selected date`), DateRangePickerHeadline empty (`Start date – End date`), DatePickerHeadline Input empty (`Entered date`), DateRangePicker Input SelectableDates not-allowed, calendar-grid weekend disable, SelectableDates / Date not allowed, DatePicker Input DateInputValidator errors, DateInputValidator year-range, DateRangePicker Input format/order errors, docked trailing DateRange, range-hero VerticalMonthsList, docked live select, docked YearPicker, single-date modal month nav, single-date modal Confirm/Cancel, range-hero connectors / Confirm/Cancel / showModeToggle / YearPicker / month nav / live start→end, modal date range input, YearPicker sibling, live Picker↔Input showModeToggle, modal date input, search filter chips, search no-results empty, two-line leading 40/20, trailing clear-X, two-line queried search rows, segmented 2dp filled search rows, TimePickerLayoutType Horizontal, docked search 32% scrim + min 240 / max ⅔, search Results / Quick results status, 24-hour ClockFace dial, search suggestion groups, contained compact→docked, 24-hour TimeInput, TimeInput 96×72 CornerLarge + ScrollDisplayModeToggle, contained search, Expressive TimeScroll, `roundedShape` CornerMedium 12 + `tonalColors()` + Inside label, list swipe LazyColumn fling, hideOnCollapse always-extended Create, header-less modal/narrow Extended FAB Create, in-flow Extended FAB Create, ContentPadding 0/44/0/44, modalExpandedShape CornerLarge 16 / ModalContainerColor SurfaceContainer, header Menu/MenuOpen + Extended FAB Create + tooltip Above, Arrangement.Bottom, hideOnCollapse slide, Arrangement.Center, in-page submenu cascade autofocus, live narrow modal 80↔220, in-flow standard WideNavigationRail 96↔220, modal collapsed width 96, live Top→Start iconPosition lerp, GPUI chip `with_animation` press, chip press interpolation / InputChip avatar / typeahead autofocus, or GPUI hover-open as immediate, or overflow/split as a single 16dp shell, or ElevatedFilterChip / unscrimmed overlay as missing inventory — they ship. Navigation drawer and **banner** stay **out** of inventory.

Highest-impact remaining gaps:

1. **Live JVM IME (`jstring`)** — host `jni_text_from_arg` uses UTF-8 `GetStringUTFChars` on Android; a live `ImeListener` on a device JVM still needs NDK + `jni-sys`.
2. **GPUI caret bounds** — `crates/gpui` has no `Window` IME-rect API. A real GPUI method needs a crate bump.
3. **Search-view morph** — AndroidX search morphs with a container transform. GPUI still has no `cx.transform`; contained expand uses height/margin/corner tokens only (no PathBuilder scale). Compact/docked is a layout swap, not a shared-element scale.
4. **`gpui::LineCap`** — `progress.rs` maps `StrokeCap::Round` to lyon until gpui re-exports `LineCap`.
5. **Nav rail `PopUp`** — OS `PopUp` on Linux/Android NativeActivity is still unproven; rail uses an inline GPUI overlay.
6. **Device APK** — no SDK/AVD/adb in this environment; `component_demo` still needs `cargo apk run` on a device/emulator.
7. **Live GPUI + Chrome recapture** — this VM cannot create a wgpu surface; Chrome `--screenshot` hangs (dump-dom works). Official-vs-catalog strips remain v19 left halves. Recapture HTML heroes (including DatePicker MonthsNavigation a11y + ScrollDisplayModeToggle SwipeVertical + DatePicker DisplayModeToggleButton a11y + TimePicker DisplayModeToggle a11y + PeriodSelector Outline shell + TimeSelector colors + TimeInput field outline + TimePickerDefaults.shapes / ScrollFieldDefaults.shape CornerLarge 16 + fullScreenContainedSearchBarColor + DatePicker WeekDays + VibrantTimePickerDialog + TimePicker ClockFaceSizeModifier 256/238/200 + DatePickerDialog CrossAxisSpacing + TimePickerCustomLayout + TimePickerDialogDefaults Title + TimeInput SupportLabelTop + YearPicker trailing HorizontalDivider + DateEntryContainer HorizontalDivider + DatePickerDialog ContainerWidth/Height + HeaderContainerHeight + TimePicker DisplaySeparator / PeriodToggleMargin + ClockFace margins + MonthsNavigation MonthYearHeight + DateRangePicker range-header chrome + DatePickerDialog DialogButtonsPadding + DateRangePickerTitlePadding / DateRangePickerHeadlinePadding + DatePickerModeTogglePadding + Date Input `InputTextFieldPadding` + empty headlines + SelectableDates + docked / range-hero / search / time heroes) when a display/screenshot pipeline works.
8. **Predictive-back** stays unimplemented. Modal rail predictive-back scale (`PredictiveBackMaxScaleXDistance` 24 / `Y` 48) needs `cx.transform` or an equivalent host scale. Nested Android `LazyColumn` overscroll glow is not in GPUI. Higher-res stills (current bundle is 128–240px) if official optical match needs print-size photos.

Do **not** reopen as missing: the v120 do-not-reopen list plus **DatePicker MonthsNavigation a11y** (Change to previous/next month; Switch to selecting a year / Swipe to select a year, or tap to switch back to selecting a day; live tooltip). Do not restore unofficial “Previous month” / “Next month”. Do not reopen **ScrollDisplayModeToggle SwipeVertical** (do not restore Input Schedule ◷ on the scroll toggle). Do not reopen **DatePicker DisplayModeToggleButton a11y** (Switch to text input mode / Switch to calendar input mode; live tooltip). Do not restore the shorter “Switch to input mode” / “Switch to calendar mode” date-toggle titles. Do not reopen **TimePickerDialogDefaults.DisplayModeToggle / ScrollDisplayModeToggle a11y** (Switch to text input mode / Switch to scroll mode / Switch to clock mode; title-row tooltip). Do not restore the shorter “Switch to input mode” time-toggle title. Do not reopen **PeriodSelector 1dp Outline shell** (52×80 vertical / 216×38 horizontal / TimeInput 52×72, CornerSmall 8, no item gap). Do not apply `periodSelectorShape` CornerFull. Do not add TimeSelector 2dp primary outline. Clock-number selected stays Primary / OnPrimary. Do not reopen TimeSelector selected / idle colors. Do not reopen TimeInput vibrant field outline (keep 96×72 CornerLarge 16). Do not reopen TimePickerDefaults.shapes / ScrollFieldDefaults.shape CornerLarge 16. Do not restore unofficial ExtraLarge 28 on those fields. Do not reopen **fullScreenContainedSearchBarColor** (compact expanded surfaceContainerLow; docked / collapsed stay surfaceContainerHigh). Do not reopen DatePicker WeekDays, VibrantTimePickerDialog, or TimePicker ClockFaceSizeModifier. Do not restore the unofficial 192dp / 0.75 ClockFace scale. Do not reopen DatePickerDialog CrossAxisSpacing 8 or TimePickerCustomLayout. Do not apply YearPicker `requiredHeight` 335 while day cells stay 40dp. Do not change TimeScroll colon offset or shipped horizontal `HORIZONTAL_GAP_DP` 24. Official mode toggle stays in the title-row demo affordance (12/24 + ScrollDisplayModeToggle); do not move it into the action row unless a host drop the format toggle. Do not widen weekday cells past 40dp (day-grid columns stay `DateContainerWidth` 40). Dial time selectors stay `TimeSelectorContainerShape` CornerSmall 8 unless a host pass `TimePickerDefaults.shapes()` onto ClockFace.

## Suggested v122 order

1. Live IME / caret / search transform / `gpui::LineCap` if an NDK or gpui API bump becomes available (otherwise skip).
2. Highest remaining Visual QA gap that does not need a crate bump (OS `PopUp` and predictive-back scale stay skipped if still unproven / no `cx.transform`). Prefer recapture or a newly published Expressive spec delta. Official `DatePickerHorizontalPadding` 12 is already the `.cal` body inset; `YearsVerticalPadding` 16 is already `YEAR_GAP_DP`. YearPicker `requiredHeight` (`RecommendedSizeForAccessibility * (MaxCalendarRows + 1) - DividerDefaults.Thickness` = 335) still assumes 48dp calendar rows — do not apply it while day cells stay 40dp. Official VibrantTimePicker field `VibrantTimeFieldWidth` 100 / `VibrantTimeFieldWidthPortrait` 132 / `VibrantTimeFieldHeight` 120, `VibrantSeparatorWidth` 16, `VibrantPeriodToggle` 56×120 / horizontal 40, `VibrantPeriodTogglePadding` 8 / Large 16, `VibrantHorizontalTimePickerGap` 52, `VibrantVerticalTimePickerGap` 12, `UncontainedTimeFieldHeight` 136 / Toggle 140, and VibrantTimePickerCustomLayout 12dp chrome would reopen shipped TimeInput 96×72, `HORIZONTAL_GAP_DP` 24, and TimePickerCustomLayout 24/24 — do not apply. Official `TimePickerDialogDefaults.MinHeightForTimePicker` 300 is a picker↔input breakpoint, not a dialog min-height. Official `PeriodTogglePaddingSmall` 4 / `PeriodTogglePaddingLarge` 16 are flag-gated (`isUpdatedTimepickerToggleEnabled`) — do not replace shipped `PeriodToggleMargin` 12. Official `ClockDialSelectorCenterContainerSize` 8 / `ClockDialSelectorTrackContainerWidth` 2 already match `HAND_HUB_DP` / `HAND_THICKNESS_DP`. Official `DateTodayContainerOutlineWidth` 1 already matches host `border_1`. Official `TimePickerDefaults.shapes().periodSelectorShape` CornerFull and TimeSelector selected 2dp primary outline apply when shapes ride the dial / period toggle — skip unless a host drop the 8dp CornerSmall period buttons. Official `SearchBarDefaults.dockedDropdownGapSize` 2 / `dockedDropdownShape` Corner 12 / `DockedExpandedWithGapTableMaxHeightScreenRatio` 1/2 belong to `ExpandedDockedSearchBarWithGap` and would reopen the shipped single-shell docked list (min 240 / max ⅔, results under the bar) — do not apply unless a host drops the combined container. Do not restore a Save/X toolbar to force range header back to 128. Do not change TimeScroll colon offset or shipped horizontal `HORIZONTAL_GAP_DP` 24.
3. APK install + Chrome/wgpu recapture of v21–v121 heroes.

Host tests: `cargo test -p gpui_material` (35), `-p gpui_android` (17), `-p material_desktop_demo` (1). Android GPUI paint is `cfg(target_os = "android")`.
